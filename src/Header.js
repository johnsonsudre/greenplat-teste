import { HeaderBox, Logo } from './styles/styles';

export const Header = () => {
  return (
    <>
      <HeaderBox id="Header">
        <Logo src="/logo-full.svg" />
      </HeaderBox>
    </>
  );
};

export default Header;
